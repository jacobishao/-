#include<iostream>
#include<algorithm>
#include<limits.h>
using namespace std;
long long sum[500][500][500][500]={INIT_MIN};
int arr[500][500];
int maxsum(int i1,int i2,int j1,int j2)
{
  if(i1<i2&&j1<j2)
    return max(maxsum(i1,i2,j1,j2-1),max(maxsum(i1,i2,j1+1,j2),max(maxsum(i1+1,i2,j1,j2),maxsum(i1,i2-1,j1,j2))),computesum(i1,i2,j1,j2));
  else if(i1==i2&&j1<j2)
    return max(maxsum(i1,i2,j1,j2-1),maxsum());


}


int main()
{
  int m,n;
  cin>>n>>m;
  for(int i=0;i<n;i++)
    for(int j=0;j<m;j++)
      {
	cin>>arr[i][j];
	cin>>sum[i][i][j][j];
      }
  cout<<maxsum(1,n,1,m)<<endl;
